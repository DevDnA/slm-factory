Commit all current changes and push to remote.

## Steps

1. **Commit**: Create a git commit with all current changes. Follow the repository's commit message conventions (Korean prefix style like `feat:`, `fix:`, `docs:`, `test:`, `perf:`, `refactor:`). Ask the user for a commit message if the intent is unclear from the changes.
2. **Push**: Push the commit to the remote repository.
