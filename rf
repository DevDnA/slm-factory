#!/usr/bin/env bash
# rf — rag-factory 간편 명령어
# 가상환경 활성화 없이 어디서든 실행할 수 있습니다.
#
# 사용법:
#   rf init my-project
#   rf tune
#   rf rag
#
# 내부적으로 uv run rag-factory를 호출합니다.

SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
exec uv run --project "$SCRIPT_DIR" rag-factory "$@"
