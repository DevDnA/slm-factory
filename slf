#!/usr/bin/env bash
# slf — slm-factory 간편 명령어
# 가상환경 활성화 없이 어디서든 실행할 수 있습니다.
#
# 사용법:
#   slf init my-project
#   slf run
#   slf serve
#
# 내부적으로 uv run slm-factory를 호출합니다.

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec uv run --project "$SCRIPT_DIR" slm-factory "$@"
